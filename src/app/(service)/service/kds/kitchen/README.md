# kitchen — KDS Cocina

Pantalla de preparación para el equipo de cocina.
Muestra en tiempo real los ítems con área `KITCHEN` en estado `QUEUED` o `PREPARING`.
Modo oscuro — diseñado para uso en pantallas de cocina.

## Rol autorizado
`STAFF_KITCHEN` — redirigido automáticamente desde `/service`.
