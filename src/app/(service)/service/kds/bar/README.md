# bar — KDS Barra

Pantalla de preparación para el equipo de barra.
Muestra en tiempo real los ítems con área `BAR` en estado `QUEUED` o `PREPARING`.
Modo oscuro — diseñado para uso en pantallas de barra.

## Rol autorizado
`STAFF_BAR` — redirigido automáticamente desde `/service`.
