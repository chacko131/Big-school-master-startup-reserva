# `onbarding`

Componentes reutilizables del flujo de onboarding del restaurante.

## Responsabilidad

Separar la capa visual compartida del onboarding para poder montar cada paso sin duplicar shell, navegación, campos y estados visuales.
