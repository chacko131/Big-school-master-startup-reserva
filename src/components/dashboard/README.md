# `dashboard`

Componentes reutilizables del panel operativo del restaurante.

## Responsabilidad

Encapsular el `header`, el `sidebar` y el shell visual del dashboard para que las páginas internas compartan la misma navegación y jerarquía de interfaz.

## Estado

Base reutilizable para el crecimiento del área privada.
